ball = Ball(initial_position=(-1000, -300.0), initial_velocity=(0, 0))
simulator.set_ball(ball)

robot = Robot(player_number=1, initial_position=(0.0, 0.0), initial_heading=3.14)
simulator.add_robot(robot)

